name: Unzip

on:
  workflow_dispatch: # Manual trigger

jobs:
  replace-repo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Delete all repo files except .git
        run: |
          shopt -s extglob
          echo "Deleting all files except .git..."
          rm -rf !( .git )

      - name: Download ks-panel.zip
        run: |
          curl -L -o ks-panel.zip https://github.com/kswarrior/panel/releases/download/main/panel.zip

      - name: Unzip ks-panel.zip into repo root
        run: |
          unzip ks-panel.zip
          # Move files to repo root if zipped folder exists
          FOLDER=$(unzip -Z -1 ks-panel.zip | head -n1 | cut -d/ -f1)
          if [ -d "$FOLDER" ]; then
            mv $FOLDER/* .
            rm -rf $FOLDER
          fi
          rm ks-panel.zip

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Replace repo files with ks-panel.zip content" || echo "No changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
